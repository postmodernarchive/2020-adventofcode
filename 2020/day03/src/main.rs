extern crate advent_lib;

// how many trees do I encounter, when I go right 3 and down 1
// for the whole "map"
// pattern repeats

fn main() {
    let lines: Vec<String> = advent_lib::file_to_vec("input.txt".to_string());

    for line in lines {
        for x in 0..2 {
            if line[x] == '#' {

            }
        }

    }
}

fn traverse()
